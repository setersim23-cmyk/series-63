node_modules/
dist/
*.local
.DS_Store
dist-single/

# Piper binary and voice model — downloaded on demand, ~150MB.
.audio-build/

# Rendered narration — 40MB, generated by scripts/build_audio.py and rebuilt
# (and cached) by the deploy workflow rather than committed.
public/audio/
